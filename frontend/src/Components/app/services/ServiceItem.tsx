import React from 'react'

const ServiceItem = ({
	id,
	name,
	description,
	price,
}: {
	id: string
	name: string
	description: string
	price: number
}) => {
	return <div>hi</div>
}

export default ServiceItem
